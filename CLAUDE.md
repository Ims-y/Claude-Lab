# Claude Code 行動規範

## 【優先順位】
セキュリティ > 正確性 > 速度

## セキュリティルール（必須）

### シークレット保護
- 認証情報ファイルは常にパスのみで参照し、中身をツールの引数・chat・PR・ログに展開するな
- 対象パターン：`*.env`, `*secret*`, `*token*`, `*credential*`, `*.pem`, `*.key`, `id_rsa*`, `id_ed25519*`, `~/.aws/*`, `~/.config/gcloud/*`, `mcp-keys.env`, `client_secret_*.json`
- `env`, `printenv`, シークレットダンプ系コマンドは**絶対に実行しない**（警告後実行も不可）
- `process.env` / `os.environ` のシークレット値を chat や外部に出力するコードを生成するな
- `git log -p`, `git show`, `git diff` の出力をそのまま chat・PR・外部サービスに貼るな

### シェル安全操作
- 破壊的コマンド（`rm -rf`, `git reset --hard`, `git push --force` 等）は**同一ターン内**で明示的に確認を得てから実行せよ
- Bashコマンドでシークレット値が stdout/stderr に出る可能性がある場合は**実行前に警告し、中止せよ**

### サブエージェント伝播
- Agent ツールでサブエージェントを呼び出す場合、本セクションのルール全項目をプロンプトに明示的に含めること

### その他
- ctx_index に token.json・client_secret_*.json・GEMINI_API_KEY の内容を絶対に渡すな
- `~/.context-mode` ディレクトリが新規作成された場合、直ちに以下を実行せよ：
  ```bash
  chmod 700 ~/.context-mode && chmod 600 ~/.context-mode/**/*.db
  ```

---

## 【context7】ライブラリ・ドキュメント取得

外部ライブラリ・フレームワークのコードを書く前に、**必ずcontext7で最新ドキュメントを取得せよ**。トレーニングデータに頼るな。

### 自動発動条件（明示指示不要）
- React / Next.js / Vue / Svelte / Astro への言及
- Supabase / Prisma / Drizzle / MongoDB 等DBライブラリ
- Tailwind / shadcn/ui 等UIライブラリ
- Node.js / Express / Fastify / Hono 等サーバーフレームワーク
- その他：インポート文に外部パッケージが含まれる実装タスク全般

### 使用ルール
```
# バージョン未指定（最新を取得）
use context7

# バージョン指定
use context7 with /vercel/next.js/v15.0.0 for app router

# 調査を別エージェントに委譲（長いタスク中にコンテキストを汚さない）
spawn docs-researcher: [調べたいライブラリと質問]
```

### 禁止事項
- トレーニングデータのみに基づくAPI・メソッド名の使用
- 「たぶんこうだったはず」という推測コードの生成
- context7未確認での deprecated パターン使用

---

## 【security-guidance】セキュリティフック

**このフックは自動動作する。警告が出たら必ず止まれ。**

### 警告が出た場合の行動規範
1. 実装を即時停止
2. 警告内容をユーザーに日本語で要約報告
3. 安全な代替実装を提示してから再開

### 検出パターンと対処法（警告文の日本語対応表）

| 検出パターン | 意味 | 安全な代替 |
|---|---|---|
| `child_process.exec()` | シェルインジェクション危険 | `execFile()` を使用 |
| `eval()` / `new Function()` | 任意コード実行危険 | JSON.parse等の安全な代替 |
| `dangerouslySetInnerHTML` | XSS（クロスサイトスクリプティング）危険 | DOMPurify でサニタイズ後に使用 |
| `innerHTML` / `document.write` | XSS危険 | `textContent` を使用 |
| `pickle.loads()` | Pythonデシリアライゼーション危険 | `json.loads()` を使用 |
| `os.system()` | Pythonコマンドインジェクション危険 | `subprocess.run(args=[])` を使用 |
| GitHub Actions `${{github.event.*}}` | CI/CDインジェクション危険 | 環境変数経由で渡す |

### セキュリティ方針
- 警告をスキップ・無視するな
- 「動けばいい」でセキュリティホールを残すな
- セキュリティと機能性が競合する場合は**必ずユーザーに確認**

---

## 【code-review】PRコードレビュー

### 使用タイミング
```bash
# PRブランチ上で実行（ターミナル出力）
/code-review

# PRにコメントとして投稿
/code-review --comment
```

### レビュー前の準備（このファイルがスコアに直結する）
- CLAUDE.md（このファイル）にプロジェクト固有のルールを明記せよ
- コーディング規約・命名規則を具体的に書け
- エラーハンドリング方針を記載せよ

### レビュー結果の解釈
| 信頼スコア | 意味 | 対応 |
|---|---|---|
| 80〜100 | 確実な問題 | **必ず修正** |
| 50〜79 | 疑わしい問題 | 確認して判断 |
| 0〜49 | 誤検知の可能性大 | 基本無視でOK |

### レビューで見るべき観点（並列エージェントが担当）
1. CLAUDE.mdルール準拠チェック
2. バグ・ロジックエラー検出
3. 過去PRとの一貫性
4. コードコメント品質
5. テスト網羅性

### PR作成前チェックリスト
- `git status` と関連diffを確認
- テスト・型チェックが存在する場合は**必須実行**（スキップする場合はその理由を明記）
- シークレットスキャンを実施し、結果を報告
- 残存リスクをユーザーに明示してから PR を提案せよ

---

## 【共通ルール】

### コード生成の順序
```
1. context7で最新ドキュメント確認（外部ライブラリを使う場合）
2. security-guidanceの警告に注意しながら実装
3. PR作成時に /code-review で最終確認
```

### 報告フォーマット（日本語必須）
- セキュリティ警告：「⚠️ セキュリティ警告：[パターン名] を検出。[理由]。代替案：[安全な実装]」
- ドキュメント取得：「📚 context7より [ライブラリ名 vX.X] の最新ドキュメントを取得しました」
- レビュー結果：「🔍 コードレビュー完了：[件数]件の問題を検出（信頼度80以上）」

### 絶対禁止
- セキュリティ警告の黙殺
- context7未確認での最新APIの断言
- 信頼スコア80未満の問題を「重大」と報告

---

## 【feature-dev】機能開発ワークフロー

**新機能を実装する前に必ず `/feature-dev` を起動せよ。いきなりコードを書くな。**

### 起動条件（以下に該当したら自動提案）
- 「〜機能を追加したい」
- 「〜を実装してほしい」
- 「新しい〜を作って」
- 既存コードへの非trivialな変更（10行超）

### 7フェーズの流れ
```
/feature-dev [機能の説明]

Phase 1: 要件の明確化（質問に正直に答えよ）
Phase 2: コードベース探索（code-explorerエージェント）
Phase 3: アーキテクチャ設計（code-architectエージェント）
Phase 4: 設計レビュー（承認してから実装開始）
Phase 5: 実装
Phase 6: 品質レビュー（code-reviewerエージェント）
Phase 7: 完了確認
```

### 禁止事項
- フェーズをスキップしての実装開始
- 設計承認前のコード生成
- 「とりあえず動けばいい」実装

---

## 【github】GitHub操作

**ターミナルでgitコマンドを打つ前に、GitHubプラグインで完結できないか確認せよ。**

### 自然言語でそのまま指示できること
```
# Issue管理
「ログイン画面のバグをIssueに起票して」
「未解決のIssue一覧を見せて」
「Issue #42をクローズして」

# PR管理
「現在のブランチでPRを作成して」
「PRにレビューコメントを追加して」
「masterへのPRをマージして」

# コード・リポジトリ
「このリポジトリの構造を分析して」
「最近のコミット履歴を見せて」
「○○ブランチのファイルを確認して」

# CI/CD・セキュリティ
「GitHub Actionsの失敗ビルドを確認して」
「Dependabotアラートを確認して」
「コードスキャンの結果を見せて」
```

### 操作前確認ルール（必須）
- `git push`, `gh pr create`, PRコメント, merge, release, workflow-dispatch の前に：アクションの内容を説明し、**同一ターン内**で明示的な承認を得てから実行せよ（事前・一括承認は無効）
- ステージング・コミット前に以下を含まないことを確認：`*.env`, `*secret*`, `*token*`, `*credential*`, `*.pem`, `*.key`, `id_rsa*`, `mcp-keys.env`, `client_secret_*.json`

### 連携ワークフロー（feature-devと組み合わせ）
```
1. /feature-dev で設計・実装
2. GitHubプラグインでPR作成
3. /code-review でレビュー実行
4. GitHubプラグインでマージ
```

---

## 【linear】タスク・プロジェクト管理

**開発作業はすべてLinearと連動させよ。口頭だけのタスクを作るな。**

### 自然言語でそのまま指示できること
```
# Issue作成・管理
「このバグをLinearにIssueとして登録して」
「現在のスプリントのタスク一覧を見せて」
「Issue [タイトル] のステータスをIn Progressに変更して」

# プロジェクト管理
「今週の未完了タスクを確認して」
「優先度Highのタスクを全部見せて」
「[機能名] のプロジェクトに新しいマイルストーンを追加して」
```

### 連携ワークフロー（全プラグイン統合）
```
実装開始前: Linearでタスクを「In Progress」に変更
実装中:     /feature-dev でコード実装
実装後:     GitHubでPR作成 → /code-review → マージ
完了時:     LinearでタスクをDoneに変更
```

---

## 【frontend-design】フロントエンドデザイン

**UI実装タスクで自動発動する。明示指示は不要。**

### 自動発動トリガー
- コンポーネント・UI作成の依頼
- CSS・スタイリングの相談
- レイアウト・デザインの実装

### 最大出力を引き出すプロンプト例
```
# 悪い例（汎用すぎる）
「ボタンコンポーネントを作って」

# 良い例（デザイン方向性を明示）
「ダークモード対応のボタンコンポーネントを作って。
モダンでプロフェッショナルな印象。アニメーション付き。
use context7でTailwindの最新APIを確認してから実装して」

# さらに良い例（用途・制約を明示）
「SaaSダッシュボード向けのサイドナビゲーションを作って。
デスクトップ・モバイル両対応、アクティブ状態あり、
アイコンはlucide-reactを使用。generic AIっぽい見た目は避けて」
```

### 禁止事項
- 「とりあえずシンプルに」という指示（スキルが半減する）
- デザイン方向性の未指定（「いい感じに」は不可）

---

## 【全プラグイン統合ワークフロー】

### 標準開発サイクル
```
① Linearでタスク確認・着手
   「Linearで今日やるべきタスクを見せて」

② feature-devで設計・実装
   /feature-dev [Linearのタスクタイトル]

③ フロントエンドならfrontend-designが自動発動
   + context7で最新ドキュメント取得

④ security-guidanceが自動監視（バックグラウンド）

⑤ GitHubでPR作成
   「現在のブランチでPRを作成して」

⑥ code-reviewでレビュー
   /code-review

⑦ マージ・Linearのタスク完了
   「PR #XX をマージして」
   「LinearのタスクをDoneにして」
```

### プラグイン優先度マトリクス
| 状況 | 使うプラグイン |
|---|---|
| 新機能着手 | Linear → feature-dev |
| UI実装 | feature-dev + frontend-design + context7 |
| PR作成・マージ | github |
| レビュー | code-review |
| バグ修正 | github（Issue確認）→ feature-dev |
| 全工程 | Linear + feature-dev + github + code-review |

---

## 【applescript-mcp】AppleScript MCP セキュリティルール

**このルールは `@peakmojo/applescript-mcp` 使用時に必ず適用する。**

### 実行禁止パターン（即時拒否・代替提案せよ）

| 禁止パターン | 理由 |
|---|---|
| `rm -rf` を含む AppleScript | 不可逆なファイル削除 |
| `security` コマンド（Keychain 操作） | 認証情報の漏洩リスク |
| `do shell script` での `sudo` 実行 | 管理者権限の不正取得 |
| `--remoteHost` / `--remoteUser` / `--remotePassword` 引数 | SSH 経由のリモート Mac 操作 |
| Mail アプリへの `send` 操作（確認なし） | なりすまし送信 |

### Mail 送信時の必須フロー

```
1. 送信先・件名・本文をユーザーに提示
2. 「送信してよいですか？」と明示的に確認
3. 許可を得てから AppleScript を実行
```

### プロンプトインジェクション対策

- 信頼できない Web ページを開いた状態で AppleScript 実行を依頼しないこと
- Web ページ要約・翻訳タスクと AppleScript 操作タスクを同時に依頼しないこと

### 付与しない macOS 権限

- フルディスクアクセス（絶対に付与しない）
- アクセシビリティ（不要な場合は付与しない）

---

## 【mcp・外部サービス】操作ルール

- MCP サーバーの認証・初期設定はユーザー承認を得てから行う（read-only 操作も MCP 認証が前提のため同様）
- Slack 投稿, GitHub コメント, 外部サービスへのデータ送信は、**送信内容を先に提示**してから実行せよ
- Context7 クエリにプロプライエタリなコードやシークレット値を含めるな
- 外部サービスへの送信前に、シークレットが混入していないか確認せよ

---

## セキュリティ絶対ルール

### キー保存の絶対ルール
- APIキーの保存先は ~/.config/mcp-secrets/ のみ
- Brain/（Obsidian同期対象）にキーを絶対に書かない
- .env.exampleにはプレースホルダーのみ（your-api-key-here）

### コミット前チェック
- git add前にAPIキーが含まれていないか必ず確認
- git commit --no-verify は絶対に使用しない
- 迷ったらコミットせずゆうすけに確認する
