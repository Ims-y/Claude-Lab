# 📖 スーパー要約システム　取扱説明書
### 〜長い文章を、脳に一番やさしい形にギュッと縮めてくれる魔法の道具〜

---

## 🌟 このシステムって何？

どんなに長くてむずかしい文章でも、**「脳に一番やさしい形にギュッと縮めてくれる魔法の道具」**です。

ただ短くするだけじゃありません。
できあがった要約を **100点満点で採点して、点数が低ければ自動で何度でも書き直してくれます。**

**たとえばこんなときに使えます👇**
- 先生からもらった長い説明プリント → 3行の核心だけ残す
- 読むのが大変な長い記事 → 誰でも読めるまとめに変換
- 英語まじりの文書 → 頭に入りやすい形に整理

---

## ✅ 始める前の準備（最初の1回だけ！）

### 🔑 パスワードを設定する

このシステムはインターネット上の AI とおしゃべりして要約を作ります。
そのための「合言葉（パスワード）」が必要です。

✅ **ステップ1：** プロジェクトのフォルダの中に `.env` というファイルを作る

✅ **ステップ2：** 以下の1行を書いて保存する

```
ANTHROPIC_API_KEY=(REDACTED-API-KEY)
```

✅ **ステップ3：** 完成！このファイルは他の人に見せないでね🔒

> パスワードがわからない場合は、このシステムを渡してくれた人に聞いてみよう。

---

## 🚀 3つの使い方（最強モード）

---

### 使い方① デモで試す（まずはここから！）

**むずかしさ：** ⭐☆☆☆☆

やることは**ターミナル（黒い画面）に1行打つだけ**です。

```
npm run デモ
```

✅ サンプル文章が自動的に読み込まれる
✅ AI が要約を作りながら文字がリアルタイムで流れてくる（映画みたい！）
✅ 採点結果と要約が画面に表示されて終わり

**こんな画面が出てきます👇**
```
=== スーパー要約システム ===

▸ 文章の構造を分析中...
  タイトル    : TypeScriptにおける型システムの設計原則
  セクション数 : 4
  合計文字数   : 321

▸ むずかしさを判定中...
  難易度         : medium（ふつう）
  おすすめテンプレ: narrative

▸ 要約を生成中（リアルタイム）...
  {"hook": "なぜ型を書くのか...

=== 要約結果 ===
（きれいに整形された要約がここに出てくる）

=== 採点結果 ===
  総合点   : 87 / 100  ← 合格！
```

---

### 使い方② 自動で要約する（⭐一番おすすめ！）

**むずかしさ：** ⭐⭐☆☆☆

**やること：** `inbox` フォルダにファイルを放り込むだけ。
あとはシステムが全部やってくれます。

---

✅ **ステップ1：** ターミナルを開いて打つ

```
npm run 監視開始
```

✅ **ステップ2：** こんな画面が出たら成功！

```
▸ 新規ドキュメント監視中: /Users/.../inbox
  対象: *.txt, *.md  |  出力: .../inbox/.summaries/
  Ctrl-C で停止
```

✅ **ステップ3：** `inbox` フォルダを開く

```
スーパー要約システム/
  └── 📁 inbox  ← ここ！（なければ自動で作られます）
```

✅ **ステップ4：** 要約したいファイルを `inbox` に放り込む

✅ **ステップ5：** 画面を見て待つだけ。こんな表示が出ます👇

```
✓ 私のレポート.txt
  テンプレート : chunked（かたまり型）
  Score    : [████████░░] 83/100  （2回書き直した）
  チャンク : 100  アンカー: 88  密度: 72
  トークン : in=1842  out=312  キャッシュ=1104
```

✅ **ステップ6：** `inbox/.summaries/` フォルダを開くと結果が出ている！

```
📁 inbox/
  └── 📁 .summaries/
        ├── 私のレポート.2026-05-07T....md    ← マークダウン形式の要約
        └── 私のレポート.2026-05-07T....json  ← くわしいデータ（採点も入ってる）
```

---

**使えるファイルの種類👇**

| 種類 | 拡張子 | どんなとき使う？ |
|---|---|---|
| 普通のテキスト | `.txt` | メモ帳で書いた文章 |
| マークダウン | `.md` | 見出しを `#` で書いた文章 |

> 💡 **ファイルを入れたまま書き直しても大丈夫！**
> 保存してから0.3秒待つと自動で再処理してくれます。書きかけのファイルが途中で処理されることはありません。

---

### 使い方③ 自分のプログラムから使う（上級者向け）

**むずかしさ：** ⭐⭐⭐⭐☆

自分で作ったプログラムにこのシステムを組み込みたい人向けです。
`スーパー要約実行` という命令（関数）を使います。

```typescript
import { スーパー要約実行 } from "./index.js";

// 要約したい文章を準備する
const doc = {
  text: "ここに長い文章を入れる...",
};

// 実行！（あとは全部自動）
const { result, passes } = await スーパー要約実行(doc);

// 結果を取り出す
console.log(result.rendered.text);     // 要約テキスト（読みやすい形）
console.log(result.rendered.markdown); // マークダウン形式の要約
console.log(result.cognitiveScore);    // 採点結果（100点満点）
console.log(`${passes}回書き直した`);  // 自動修正した回数
```

✅ むずかしさの判定・AI への質問・自動書き直しまで、全部やってくれます
✅ 結果は `result.rendered.text` に入っています

---

## 📊 「採点結果」の読み方

このシステムには **「読みやすさの仕分け人」** が内蔵されています。
要約が完成するたびに **100点満点で採点**し、点数が低ければ自動で書き直します。

### 🏆 点数表

| 点数 | 評価 | 意味 |
|---|---|---|
| 🟢 **80点以上** | **合格！** そのまま読んでOK | 脳に一番やさしい形になっています |
| 🟡 **70〜79点** | **もう少し** | システムが自動で書き直し中… |
| 🔴 **70点以下** | **要改善** | ちょっと読みにくいかも。何度でも書き直します |

---

### 🔍 採点の4つのポイント（仕分けの基準）

システムは4つの観点で採点しています。それぞれの「裏方の仕事」を紹介します。

| 裏方の担当 | やっていること | 満点をとるコツ |
|---|---|---|
| **かたまり番**（チャンク点） | 情報をいくつのかたまりに分けたかをチェック。多すぎると頭が疲れる | 3〜4個にきれいにまとめる |
| **先頭番**（アンカー点） | 一番大事な結論が最初に書いてあるかチェック | 「結論から先に！」を守る |
| **スリム番**（密度点） | 文章が長すぎないかチェック。ムダな言葉が多いと減点 | 必要なことだけ残す |
| **ドラマ番**（緊張解決点） | 「問題 → 解決」の流れがあるかチェック | 起承転結のある構成にする |

---

### 🎯 テンプレートって何？

このシステムは、文章の種類によって**5つの書き方**（テンプレート）を使い分けます。
仕分け人が自動でえらんでくれるので、何もしなくてOKです。

| テンプレート名 | どんな文章に使う？ | 特徴 |
|---|---|---|
| **BLUF**（結論先出し型） | 短い報告・決断が必要な文書 | 一番大事なことを最初に書く |
| **Layered**（重ね読み型） | ニュースや記事・ウィキ | 「1行版 → 箇条書き → くわしい説明」の3段階 |
| **SCQA**（ドラマ型） | 提案書・分析レポート | 「状況 → 問題 → 疑問 → 答え」の流れ |
| **Chunked**（かたまり型） | 技術文書・マニュアル | 情報を3〜4つのかたまりに整理 |
| **Narrative**（物語型） | 長文記事・エッセイ | 「つかみ → 背景 → 発見 → まとめ」の流れ |

---

## ⚡ すごいところ（MacBook M5 MAX 専用）

このシステムは **MacBook M5 MAX** の化け物級パワーをフルに使うように設計されています。

### 普通のツールとの違い

```
🐢 普通のツール（1つずつ順番に処理）：
   ファイル1 → 処理 → 完了
                       ファイル2 → 処理 → 完了
                                           ファイル3 → 処理...

🚀 スーパー要約システム（M5 MAX の力で同時に処理）：
   ファイル1 ─┐
   ファイル2 ─┤
   ファイル3 ─┼→ 同時に処理！ → まとめて完了💨
   ファイル4 ─┤
   ファイル5 ─┘
```

✅ M5 MAXの多核チップが全力で働く
✅ 最大5ファイルを同時に処理（API への負担はきっちりコントロール済み）
✅ 自動書き直しも並行して走る
✅ 大量ファイルでも処理時間が激減

> 💡 M5 MAX じゃないパソコンでも動きます。ただし同時処理数が少なめになります。

---

## ❓ こんなときはどうする？

**Q. `inbox` フォルダがないと言われた！**
→ `npm run 監視開始` を打てば自動で作られます。

**Q. 点数が低くて何度も書き直されている…**
→ それが正常動作！むずかしい文章ほど、最大5回まで自動で書き直してベストを目指します。

**Q. 処理が止まったまま動かない**
→ `Ctrl + C` で止めて、もう一度 `npm run 監視開始` を打ってください。

**Q. 英語の文章も使える？**
→ 使えます！日本語・英語・まざった文章もすべてOK。言語は自動で判断します。

**Q. 要約がへんな文章になった**
→ `.summaries/` フォルダの中の `.json` ファイルを見てください。採点の内訳が書いてあります。

---

## 🗂️ システムの中身（気になる人向け）

```
スーパー要約システム/
  ├── 📁 src/
  │   ├── 🔬 構造分析器.ts       文章の骨格（見出しや段落）を調べる担当
  │   ├── 🌡️ 難易度判定器.ts     文章のむずかしさを数値で測る担当
  │   ├── 🤖 要約実行部.ts       AI に接続して実際に要約を作る担当
  │   ├── ✍️ 自己修正プロセッサ.ts 点数が低いと自動で書き直す担当
  │   ├── 🏅 採点ロジック.ts     100点満点で採点する担当（仕分け人の正体）
  │   └── ⚡ 一括処理エンジン.ts  M5 MAXのパワーで複数ファイルを同時処理する担当
  │
  └── 📁 inbox/                  ← ファイルをここに放り込む！
        └── 📁 .summaries/       ← 結果がここに出てくる（自動で作られる）
```

---

*🤖 このシステムは Anthropic 社の Claude AI の力を借りて動いています。*
*📅 最終更新：2026年5月*
